export const LOGIN_PATH = "/login";
export const HOME_PATH = "/";

/** Placeholder para funcionalidades no disponibles en la demo (FR-009). */
export const DEMO_UNAVAILABLE_PATH = "/demo-unavailable";

/** En esta feature apunta al mismo placeholder hasta existir US de transferencias. */
export const TRANSFER_PATH = DEMO_UNAVAILABLE_PATH;
